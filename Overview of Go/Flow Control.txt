pada go, sintax if-else sama dengan c/c++/php

switch var_name{
	case "cat":
		//do something
	case "dog":
		//do something
	default:
		//do something
}

// looping basic
perhatikan bahwa sintaks seperti C, tapi tidak menggunakan ()
for i:=0; i<10; i++{
}

looping array/slice:
animals := []string{"dog", "fish", "cat"}
for _, animal := range animals{
	// akses var _ atau animal
}

# pada code diatas, "_" berisi index boleh tidak digunakan

kita dapat melakukan range pada: slice, map, string
untuk sinyaksnya sama semua
jika string maka akan direpresentasikan dalam decimal value